# @tetorial/input — QUESTIONS — **전 항목 처리 완료 (2026-07-12, 총괄)**

> 반영: input.md §2·§3-4·§4 개정 + 개정 이력 §7 / DOM 어댑터 모디파이어 인코딩 구현(총괄) /
> DEFAULT_HANDLING·DEFAULT_KEYS export.

## Q1. 메타 액션의 모디파이어 조합 표현

- 진행 기본값: 코어는 키 문자열 불투명 매칭, 메타 기본 바인딩 비움.
> **총괄: (a)+(b) 절충으로 확정.** 코어 불투명 유지 승인. 정규 인코딩(`"Ctrl+KeyZ"` — Ctrl/Meta만
> 접두, Alt·Shift는 게임 키로 쓰이므로 제외)은 **input DOM 어댑터가 소유·구현**한다 — 명세 §2
> 명문화 + 어댑터에 인코딩·keyup 짝맞춤(스턱 키 방지) 구현·테스트 완료(총괄). 기본 메타 조합
> 값의 결정은 apps/web 설정 UI 몫(rebind 주입).

## Q2. 정지(suspend) 중 유지되는 메타 액션 범위

- 진행 기본값: addPage만 발화, undo/redo 정지. I-3은 엔진 호출 기준.
> **총괄: 고정 기본값 승인.** 명세 §3-4에 v1 고정 정책으로 명문화. 런타임 옵션 승격은 W3에서
> 필요가 확인되면 개정. I-3의 엔진 호출 기준 해석도 명세에 명시했다.

## Q3. 유한 SDF 환산 상수 · 기본값 export

- SDF: `SDF_BASE_MS = 500` 단일 상수.
> **총괄: 유지 승인.** 미결 항목(구현 후 체감 확정) 그대로 — 튜닝 시점에 상수만 조정.
- 기본값 export 제안.
> **총괄: 승인.** `DEFAULT_HANDLING`·`DEFAULT_KEYS`를 공개했다(명세 §4 개정, index·README 반영).
> apps/web 설정 UI의 "기본값 표시/초기화"가 소비처.
