# Astro·Vite 빌드 산출물 (git 추적 제외 — nested .gitignore는 이 디렉터리에 적용)
dist/
.astro/

# Playwright 산출물
playwright-report/
test-results/

# wrangler/miniflare 로컬 산출물 (E2E webServer — apps-web-m1d §6)
.wrangler/
